#include<stdio.h>
int main(void)
{
   int i;
 
  printf ("キーボードから1以上の数字を入力してください。\n");
  int a;
  do{
	scanf("%d\n",&a);
  }while(a <= 0);
  for (i=1;i<a;i++){
	if (a%i == 0)printf ("%d ",i);
  }
  printf ("\n");
  return 0;
}
